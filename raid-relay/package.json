{
  "name": "tt2-raid-relay",
  "version": "1.0.0",
  "description": "Relay server for TT2 Toolkit raid status feature, forwarding GameHive API requests that the browser can't call directly (CORS + custom WebSocket headers), plus a lightweight background watcher that keeps the last 10 attacks so the page can show recent history even before it was opened.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "socket.io-client": "^4.7.5"
  }
}
